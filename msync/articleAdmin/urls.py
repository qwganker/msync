from django.conf.urls import url
from .views import *

urlpatterns = [
    # url(r"^vm/expiretime$", VMinstanceExtendExpiretimeView.as_view()),
    # url(r"^vm/info/$", VMinstanceInfoView.as_view()),
    # url(r"^vm/operator$", VMinstanceOperator.as_view()),
    # url(r"^vm$", VMinstanceView.as_view()),
]
